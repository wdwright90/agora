# Repository skills

Add a skill here when a specific, repeated project workflow benefits from reusable instructions or supporting scripts. Each skill gets its own directory with a `SKILL.md` and concise trigger description.

Keep general working rules in the root `AGENTS.md` and project design in `docs/`. Link to canonical documentation instead of copying it into skills. This directory contains no executable skills yet.
